CREATE TABLE PROBE.DSSP_FILE_TRANS_OPERTION (
  FILE_TRANS_OPERTION_ID VARCHAR NOT NULL,
  RECORD_TIME BIGINT,
  OTHER_ID VARCHAR,
  FROM_IP VARCHAR,
  FROM_PORT INTEGER,
  FROM_MAC VARCHAR,
  TARGET_IP VARCHAR,
  TARGET_PORT INTEGER,
  TARGET_MAC VARCHAR,
  COMMOND VARCHAR,
  RSP_CODE VARCHAR,
  RSP_DETAIL VARCHAR,
  TRANS_MODEL VARCHAR,
  FILE_NAME VARCHAR,
  FILE_SIZE BIGINT,
  FILE_PATH VARCHAR,
  TRANS_RATE FLOAT,
  TRANS_TIME BIGINT,
  CONN_MODE VARCHAR,
  CONTROL_PORT VARCHAR,
  DATA_PORT VARCHAR,
  TRANS_DIRECTION INTEGER,
  FILE_DETAIL VARCHAR,
  SESSION_ID VARCHAR,
  REQ_BYTES INTEGER,
  RSP_BYTES INTEGER,
  REQ_PACKETS INTEGER,
  RSP_PACKET INTEGER,
  RETRANS_REQKTS INTEGER,
  RETRANS_REQBYTES INTEGER,
  RETRANS_PSPPKTS INTEGER,
  RETRANS_PSPBYTES INTEGER,
  REQ_TIME BIGINT,
  RSP_TIME BIGINT,
  APM_FLAG VARCHAR,
  SOURCE_TYPE VARCHAR,
  FROM_REGION_ID VARCHAR,
  TARGET_REGION_ID VARCHAR,
  DATA_LEVEL VARCHAR,
  CLASS_CODE_INDUSTRY VARCHAR,
  CLASS_CODE_SUBJECT VARCHAR,
  CLASS_CODE_AREA VARCHAR,
  SENSITIVE_FLAG VARCHAR,
  DATA_ELEMENT_CODE VARCHAR
);