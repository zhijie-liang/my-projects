CREATE TABLE PROBE.DSSP_WEB_REQ (
  WEB_REQ_ID VARCHAR NOT NULL,
  OTHER_ID BIGINT,
  RECORD_TIME BIGINT,
  FROM_IP VARCHAR,
  FROM_PORT INTEGER,
  FROM_MAC VARCHAR,
  TARGET_IP VARCHAR,
  TARGET_PORT INTEGER,
  TARGET_MAC VARCHAR,
  HTTP_TYPE INTEGER,
  URL VARCHAR,
  HTTP_VERSION INTEGER,
  RSQ_CODE INTEGER,
  REQ_STATU_DETAIL VARCHAR,
  LOAD_TIME BIGINT,
  CLIENT_OS INTEGER,
  CLIENT_EXPLORER INTEGER,
  CLIENT_DEVICE INTEGER,
  REQ_BYTES BIGINT,
  RSP_BYTES BIGINT,
  REQ_PACKETS BIGINT,
  RSP_PACKET BIGINT,
  RETRANS_REQKTS BIGINT,
  RETRANS_REQBYTES BIGINT,
  RETRANS_PSPPKTS BIGINT,
  RETRANS_PSPBYTES BIGINT,
  REQ_TIME BIGINT,
  RSP_TIME BIGINT,
  APM_FLAG VARCHAR,
  SOURCE_TYPE VARCHAR,
  FROM_REGION_ID VARCHAR,
  TARGET_REGION_ID VARCHAR,
  DATA_LEVEL VARCHAR,
  CLASS_CODE_INDUSTRY VARCHAR,
  CLASS_CODE_SUBJECT VARCHAR,
  CLASS_CODE_AREA VARCHAR,
  SENSITIVE_FLAG VARCHAR,
  DATA_ELEMENT_CODE VARCHAR,
  SYSTEM_CODE VARCHAR,
  REQ_HEADERS VARCHAR,
  REP_HEADERS VARCHAR,
  REQ_BODY VARCHAR,
  RSP_BODY VARCHAR
);