CREATE TABLE PROBE.DSSP_WEB_PAGE (
  WEB_PAGE_ID VARCHAR NOT NULL,
  RECORD_TIME BIGINT,
  FROM_IP VARCHAR,
  FROM_PORT INTEGER,
  FROM_MAC VARCHAR,
  TARGET_IP VARCHAR,
  TARGET_PORT INTEGER,
  TARGET_MAC VARCHAR,
  HTTP_TYPE INTEGER,
  URL VARCHAR,
  HTTP_VERSION INTEGER,
  RES_STATUS INTEGER,
  RES_DETAIL VARCHAR,
  REQ_TIME BIGINT,
  RSP_TIME BIGINT,
  PAGE_TIME BIGINT,
  LOAD_TIME BIGINT,
  CLIENT_OS INTEGER,
  CLIENT_EXPLORER INTEGER,
  CLIENT_DEVICE INTEGER,
  REQ_BODY VARCHAR,
  RSP_BODY VARCHAR,
  REQ_COUNT INTEGER,
  REQ_BYTES BIGINT,
  RSP_BYTES BIGINT,
  PAGE_ID BIGINT,
  APM_FLAG VARCHAR,
  SOURCE_TYPE VARCHAR,
  FROM_REGION_ID VARCHAR,
  TARGET_REGION_ID VARCHAR,
  DATA_LEVEL VARCHAR,
  CLASS_CODE_INDUSTRY VARCHAR,
  CLASS_CODE_SUBJECT VARCHAR,
  CLASS_CODE_AREA VARCHAR,
  SENSITIVE_FLAG VARCHAR,
  DATA_ELEMENT_CODE VARCHAR,
  SYSTEM_CODE VARCHAR
);