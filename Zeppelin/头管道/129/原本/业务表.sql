CREATE TABLE PROBE.DSSP_REMOTE_CONTROL_EXEC (
  REMOTE_CONTROL_EXEC_ID VARCHAR NOT NULL,
  RECORD_TIME BIGINT,
  OTHER_ID VARCHAR,
  FROM_IP VARCHAR,
  FROM_PORT INTEGER,
  FROM_MAC VARCHAR,
  TARGET_IP VARCHAR,
  TARGET_PORT INTEGER,
  TARGET_MAC VARCHAR,
  EXEC_COMMOND VARCHAR,
  EXEC_RESULT VARCHAR,
  SESSION_ID BIGINT,
  FAILED_INFO INTEGER,
  REQ_BYTES INTEGER,
  RSP_BYTES INTEGER,
  REQ_PACKETS INTEGER,
  RSP_PACKET INTEGER,
  RETRANS_REQKTS INTEGER,
  RETRANS_REQBYTES INTEGER,
  RETRANS_PSPPKTS INTEGER,
  RETRANS_PSPBYTES INTEGER,
  REQ_TIME BIGINT,
  RSP_TIME BIGINT,
  APM_FLAG VARCHAR,
  SOURCE_TYPE VARCHAR,
  FROM_REGION_ID VARCHAR,
  TARGET_REGION_ID VARCHAR,
  DATA_LEVEL VARCHAR,
  CLASS_CODE_INDUSTRY VARCHAR,
  CLASS_CODE_SUBJECT VARCHAR,
  CLASS_CODE_AREA VARCHAR,
  SENSITIVE_FLAG VARCHAR,
  DATA_ELEMENT_CODE VARCHAR
);