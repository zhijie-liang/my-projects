CREATE TABLE PROBE.DSSP_DATA_BASE_LOGIN (
  DATA_BASE_LOGIN_ID VARCHAR NOT NULL,
  RECORD_TIME BIGINT,
  OTHER_ID VARCHAR,
  CLIENT_IP VARCHAR,
  CLIENT_PORT INTEGER,
  CLIENT_MAC VARCHAR,
  FROM_IP VARCHAR,
  FROM_PORT INTEGER,
  TARGET_IP VARCHAR,
  TARGET_PORT INTEGER,
  APPLICATION_ACCOUNT VARCHAR,
  DATA_BASE_ACCOUNT VARCHAR,
  DATA_BASE_NAME VARCHAR,
  DATA_BASE_TYPE VARCHAR,
  DATA_BASE_VERSION VARCHAR,
  LOGIN_TIME BIGINT,
  LOGIN_STATUS INTEGER,
  LOGIN_FAILED_INFO VARCHAR,
  LOGIN_ERR_CODE VARCHAR,
  LOGIN_TYPE VARCHAR,
  SESSION_ID VARCHAR,
  APM_FLAG VARCHAR,
  SOURCE_TYPE VARCHAR,
  LOGIN_SUCCESS_TIME BIGINT
);