CREATE TABLE PROBE.DSSP_FILE_TRANS_LOGIN (
  FILE_TRANS_LOGIN_ID VARCHAR NOT NULL,
  RECORD_TIME INTEGER,
  OTHER_ID VARCHAR,
  FROM_IP VARCHAR,
  FROM_PORT INTEGER,
  FROM_MAC VARCHAR,
  TARGET_IP VARCHAR,
  TARGET_PORT INTEGER,
  TARGET_MAC VARCHAR,
  USER_LOGIN_NAME VARCHAR,
  USER_LOGIN_P_W_D VARCHAR,
  LOGIN_FLAG VARCHAR,
  DOWN_FILE_COUNT INTEGER,
  UP_FILE_COUNT INTEGER,
  LOGIN_TIME BIGINT,
  SEESION_ID VARCHAR,
  APM_FLAG VARCHAR,
  SOURCE_TYPE VARCHAR,
  FROM_REGION_ID VARCHAR,
  TARGET_REGION_ID VARCHAR,
  DATA_LEVEL VARCHAR,
  CLASS_CODE_INDUSTRY VARCHAR,
  CLASS_CODE_SUBJECT VARCHAR,
  CLASS_CODE_AREA VARCHAR,
  SENSITIVE_FLAG VARCHAR,
  DATA_ELEMENT_CODE VARCHAR
);